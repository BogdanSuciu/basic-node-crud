/**
 * id you don't have config folder in root - create it
 * dbConfig - create dbConfig.js in config folder with the following details
 const db = {
   host: <local enviroment(127.0.0.1)>,
   port: <listening port>,
   database: <database name>,
   user: <database user>,
   password: <database password>
 };

 module.exports = db;
 */
